{{- if .Values.global.backend.autoscaling.enabled }}
apiVersion: autoscaling/v2beta1
kind: HorizontalPodAutoscaler
metadata:
  name: {{ include "backend.fullname" . }}
  labels:
    {{- include "backend.labels" . | nindent 4 }}
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: {{ include "backend.fullname" . }}
  minReplicas: {{ .Values.global.backend.autoscaling.minReplicas }}
  maxReplicas: {{ .Values.global.backend.autoscaling.maxReplicas }}
  metrics:
    {{- if .Values.global.backend.autoscaling.targetCPUUtilizationPercentage }}
    - type: Resource
      resource:
        name: cpu
        targetAverageUtilization: {{ .Values.global.backend.autoscaling.targetCPUUtilizationPercentage }}
    {{- end }}
    {{- if .Values.global.backend.autoscaling.targetMemoryUtilizationPercentage }}
    - type: Resource
      resource:
        name: memory
        targetAverageUtilization: {{ .Values.global.backend.autoscaling.targetMemoryUtilizationPercentage }}
    {{- end }}
{{- end }}
